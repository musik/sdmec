-if data.nil?
-else
  .pics-o
    %ul.thumbnails
      -data.each do |r|
        %li.thumbnail
          =link_to image_tag(r["tbUrl"],:height=>80,:alt=>r["title"]),r["originalContextUrl"],:title=>r["title"],:rel=>"nofollow",:target=>"blank"