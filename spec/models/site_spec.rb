require 'spec_helper'

describe Site do
  #it "should build store" do
    #store = Store.create :nick=>'woha'
    #store2 = Store.create :nick=>'woha2'
    #site = Site.create :name=>'site',:slug=>'sitees'
    #site.shops << store
    #SiteStore.where(:site_id=>site,:store_id=>store).update_all :priority=>5
    #site.shops << store
    #site.shops.count.should eq(1)
    #site.stores << store2
    #SiteStore.where(:site_id=>site,:store_id=>store2).update_all :priority=>3
    #site.shops.order("sites_stores.priority asc").first.should eq(store2)
    #site.shops.priority_asc.first.should eq(store2)
    #site.shops.with_priority.first.priority.should eq(5)
  #end
end
